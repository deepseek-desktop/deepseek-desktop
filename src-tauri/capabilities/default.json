{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-shell",
  "description": "Minimal capability set for the local desktop shell. The embedded workbench webview receives no Tauri capabilities.",
  "webviews": ["main", "desktop-menu"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default"
  ]
}
